import React, { Component } from 'react'
import Padash from '../../components/dash/Padre'

export default class dash extends Component {
    render() {
        return (
        < div>
            <Padash />
        </div>
        )
    }
}
